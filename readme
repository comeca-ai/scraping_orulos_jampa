# Projeto de Análise de Imóveis em João Pessoa

Este projeto realiza o scraping, análise e visualização de dados de imóveis da cidade de João Pessoa, disponíveis no site Orulo.

## Como executar

1. **Instale as dependências:**

   ```bash
   pip install -r requirements.txt
   ```

2. **Execute o scraper:**

   ```bash
   python scraper.py
   ```

   Isso irá gerar os arquivos `imoveis.json` e `imoveis.csv`.

3. **Visualize a análise:**

   Abra o notebook `analise.ipynb` em um ambiente Jupyter para ver a análise exploratória dos dados.

4. **Visualize o dashboard:**

   Abra o arquivo `dashboard.html` em seu navegador para ver os gráficos interativos.

## Arquivos

- `scraper.py`: Script para fazer o scraping dos dados do site Orulo.
- `analise.ipynb`: Notebook com a análise exploratória dos dados.
- `dashboard.html`: Página web com os dashboards.
- `imoveis.json`: Dados dos imóveis em formato JSON.
- `imoveis.csv`: Dados dos imóveis em formato CSV.
- `readme`: Este arquivo.
- `requirements.txt`: Lista de dependências do projeto.
